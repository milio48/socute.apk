name: Build & Release Socute (Dual)

on:
  push:
    tags:
      - "v*" 
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.19.0'

      - name: Generate Project & Install Dependencies
        run: |
          # 1. Create project structure
          flutter create . --project-name socute --org com.socute
          flutter config --no-analytics
          
          # 2. Install required dependencies explicitly to avoid missing package errors
          flutter pub add dio archive path_provider device_info_plus permission_handler device_apps flutter_launcher_icons

      # --- GENERATE APP ICON ---
      - name: Setup & Generate App Icon
        run: |
          # Check if custom icon exists
          if [ -f "icon.png" ]; then
            echo "Custom icon found! Generating..."
            
            # Create config file using printf to avoid EOF syntax errors
            printf "flutter_icons:\n  android: \"ic_launcher\"\n  ios: true\n  image_path: \"icon.png\"\n  min_sdk_android: 21\n" > flutter_launcher_icons.yaml
            
            # Run the icon generator
            flutter pub run flutter_launcher_icons
          else
            echo "No icon.png found. Using default Flutter logo."
          fi

      # --- PERMISSION FIX (MANUAL MANIFEST OVERWRITE) ---
      - name: Overwrite AndroidManifest.xml
        run: |
          # Remove the default manifest generated by 'flutter create'
          rm android/app/src/main/AndroidManifest.xml
          
          # Write a new Manifest with all required permissions
          cat <<EOF > android/app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.socute">
              <uses-permission android:name="android.permission.INTERNET"/>
              <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
              <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
              <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
              <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>
              
              <application
                  android:label="SoCute"
                  android:name="\${applicationName}"
                  android:icon="@mipmap/ic_launcher">
                  <activity
                      android:name=".MainActivity"
                      android:exported="true"
                      android:launchMode="singleTop"
                      android:theme="@style/LaunchTheme"
                      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
                      android:hardwareAccelerated="true"
                      android:windowSoftInputMode="adjustResize">
                      <meta-data
                        android:name="io.flutter.embedding.android.NormalTheme"
                        android:resource="@style/NormalTheme"
                        />
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  <meta-data
                      android:name="flutterEmbedding"
                      android:value="2" />
              </application>
          </manifest>
          EOF

      - name: Install Dart Dependencies
        run: flutter pub get

      # ========================================================
      # PHASE 1: BUILD DEBUG VERSION
      # ========================================================
      - name: Build Debug APK
        run: flutter build apk --debug --no-tree-shake-icons

      - name: Rename Debug APK
        run: mv build/app/outputs/flutter-apk/app-debug.apk build/app/outputs/flutter-apk/socute-debug.apk

      # ========================================================
      # PHASE 2: BUILD RELEASE VERSION
      # ========================================================
      - name: Build Release APK
        run: flutter build apk --release --no-tree-shake-icons

      - name: Rename Release APK
        run: mv build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/socute-release.apk

      # ========================================================
      # UPLOAD BOTH ARTIFACTS
      # ========================================================
      - name: Create Release & Upload ALL APKs
        uses: ncipollo/release-action@v1
        with:
          # Uploading both Debug and Release APKs
          artifacts: "build/app/outputs/flutter-apk/socute-debug.apk,build/app/outputs/flutter-apk/socute-release.apk"
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            Auto-generated release.
            
            📂 **Files:**
            - `socute-release.apk` : Lightweight version for daily use (~20MB).
            - `socute-debug.apk` : Heavy version with full logs for testing/debugging (~140MB).
          allowUpdates: true
          makeLatest: true