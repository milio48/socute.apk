name: Build & Release Socute

on:
  push:
    tags:
      - "v*" 
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.19.0'

      - name: Generate Project & Install Dependencies
        run: |
          # Create project
          flutter create . --project-name socute --org com.socute
          flutter config --no-analytics
          
          # Install libraries (termasuk library pembuat icon)
          flutter pub add dio archive path_provider device_info_plus permission_handler device_apps flutter_launcher_icons

      - name: Setup & Generate App Icon
        run: |
          if [ -f "icon.png" ]; then
            echo "Custom icon found! Generating..."
            
            printf "flutter_icons:\n  android: \"ic_launcher\"\n  ios: true\n  image_path: \"icon.png\"\n  min_sdk_android: 21\n" > flutter_launcher_icons.yaml
            
            flutter pub run flutter_launcher_icons
          else
            echo "No icon.png found. Using default Flutter logo."
          fi

      # --- OVERWRITE MANIFEST (Permission Fix) ---
      - name: Overwrite AndroidManifest.xml
        run: |
          rm android/app/src/main/AndroidManifest.xml
          
          cat <<EOF > android/app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.socute">
              <uses-permission android:name="android.permission.INTERNET"/>
              <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
              <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
              <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
              <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>
              
              <application
                  android:label="SoCute"
                  android:name="\${applicationName}"
                  android:icon="@mipmap/ic_launcher">
                  <activity
                      android:name=".MainActivity"
                      android:exported="true"
                      android:launchMode="singleTop"
                      android:theme="@style/LaunchTheme"
                      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
                      android:hardwareAccelerated="true"
                      android:windowSoftInputMode="adjustResize">
                      <meta-data
                        android:name="io.flutter.embedding.android.NormalTheme"
                        android:resource="@style/NormalTheme"
                        />
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  <meta-data
                      android:name="flutterEmbedding"
                      android:value="2" />
              </application>
          </manifest>
          EOF

      - name: Install Dart Dependencies
        run: flutter pub get

      - name: Build Debug APK
        run: flutter build apk --debug --no-tree-shake-icons

      - name: Rename APK to socute.apk
        run: mv build/app/outputs/flutter-apk/app-debug.apk build/app/outputs/flutter-apk/socute.apk

      - name: Create Release & Upload APK
        uses: ncipollo/release-action@v1
        with:
          artifacts: "build/app/outputs/flutter-apk/socute.apk"
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: "Auto-generated release for Socute APK (Debug + Custom Icon)."
          allowUpdates: true
          makeLatest: true